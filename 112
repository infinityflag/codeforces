#include <iostream>
using namespace std;

int W,H;
int w,h;
int x1, y1, x2,y2;

void solve(){
	if(x2-x1 + w <= W){
		ans = min(ans, max(0, w-x1));
		ans = min(ans, max(0, x2-(W-w));
	}
	if(y2-y2 + h <= H){
		ans = min(ans, max(0,h-y1));  
		ans = min(ans, max(0,y2-(H-h));
	}
	if(ans == INF){
		
	}
	
}



int main() {
	// your code goes here
	int t;
	cin >> t;
	while(t--){
		int W,H;
		cin >> W >> H;
	}
	return 0;
}
